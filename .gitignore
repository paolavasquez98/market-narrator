# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Tool caches
.pytest_cache/
.ruff_cache/

# Keys
.env

# Data (regenerated by the ingestion pipeline; too large/derived to version)
data/raw/
data/processed/
!data/.gitkeep

# OS cruft
.DS_Store

# Eval outputs are versioned on purpose (evidence for the evaluation writeup)
!eval/results/.gitkeep

# Private, teaching-oriented learning notes (see docs/learning/*_learning.md) --
# not docs/ as a whole, which also holds public docs (PROJECT_PLAN.md,
# ticker_universe.md, deployment.md) that need to be tracked and visible
# to anyone reviewing this repo.
docs/learning/